<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Чип тега — компактный Border с именем тега. Цвет фона прозрачно-тонированный,
        рамка цветная по Tag.Color. Используется в карточке трека, фильтр-панели и
        окне управления тегами.
        Ожидаемый DataContext: Tag (Id, Name, Color?). Если Color=null — используем
        акцентный цвет приложения.
    -->
    <Style x:Key="TagChipStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="Margin" Value="0,0,6,6" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="#2616161F" />
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Текст внутри чипа: компактный, белый. -->
    <Style x:Key="TagChipTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

</ResourceDictionary>
