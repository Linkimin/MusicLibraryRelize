<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:MusicLibrary.Converters">
    <!-- Конвертеры объявлены здесь, потому что MultiBinding.Converter резолвится при загрузке шаблона
         и не видит ключей, добавленных в родительский ResourceDictionary после этого MergedDictionary. -->
    <converters:TrackIdentityMatchConverter x:Key="TrackIdentityMatchConverter" />
    <converters:TrackTagsFirstNMultiConverter x:Key="LocalTrackTagsFirstNMultiConverter" />
    <converters:TrackTagsOverflowCountMultiConverter x:Key="LocalTrackTagsOverflowCountMultiConverter" />
    <converters:TrackTagsOverflowVisibilityMultiConverter x:Key="LocalTrackTagsOverflowVisibilityMultiConverter" />

    <DataTemplate x:Key="TrackCardTemplate">
        <Border x:Name="CardRoot" Padding="14" CornerRadius="12" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource GoldBorderBrush}" BorderThickness="1" Cursor="Hand">
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick"
                              Command="{Binding DataContext.PlayTrackCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                              CommandParameter="{Binding}" />
            </Border.InputBindings>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="64" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="22" />
                </Grid.ColumnDefinitions>
                <Border Width="64" Height="64" CornerRadius="10">
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding CoverPath}" Stretch="UniformToFill" />
                    </Border.Background>
                </Border>
                <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Title}" Foreground="{StaticResource ForegroundBrush}" FontWeight="SemiBold" FontSize="16" TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding Artist}" Foreground="{StaticResource MutedForegroundBrush}" FontSize="13" TextTrimming="CharacterEllipsis" Margin="0,3,0,8" />
                    <StackPanel Orientation="Horizontal">
                        <Border Background="#1AD4A574" BorderBrush="{StaticResource GoldBorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8,3">
                            <TextBlock Text="{Binding Genre}" Foreground="{StaticResource PrimaryBrush}" FontSize="12" />
                        </Border>
                        <TextBlock Text="{Binding DurationText}" Foreground="{StaticResource MutedForegroundBrush}" FontSize="12" Margin="10,3,0,0" />
                    </StackPanel>
                    <!-- Чипы тегов трека: до 3 inline + «+N» overflow-пилюля. Snapshot, не live —
                         добавление тега в позицию ≥ 4 не отразится до перевыделения. MVP-ограничение. -->
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0" VerticalAlignment="Center">
                        <ItemsControl>
                            <ItemsControl.ItemsSource>
                                <MultiBinding Converter="{StaticResource LocalTrackTagsFirstNMultiConverter}" ConverterParameter="3">
                                    <Binding Path="Id" />
                                    <Binding Path="DataContext.TagsByTrackId" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                </MultiBinding>
                            </ItemsControl.ItemsSource>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource TagChipStyle}" Padding="6,2" Margin="0,0,4,0">
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource TagChipTextStyle}" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- «+N» overflow-пилюля: видна только когда тегов больше трёх -->
                        <Border Background="#1F1F2E" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1"
                                CornerRadius="8" Padding="6,2" VerticalAlignment="Center">
                            <Border.Visibility>
                                <MultiBinding Converter="{StaticResource LocalTrackTagsOverflowVisibilityMultiConverter}" ConverterParameter="3">
                                    <Binding Path="Id" />
                                    <Binding Path="DataContext.TagsByTrackId" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                </MultiBinding>
                            </Border.Visibility>
                            <TextBlock Foreground="{StaticResource PrimaryBrush}" FontSize="11" FontWeight="SemiBold">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="+{0}" Converter="{StaticResource LocalTrackTagsOverflowCountMultiConverter}" ConverterParameter="3">
                                        <Binding Path="Id" />
                                        <Binding Path="DataContext.TagsByTrackId" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </StackPanel>
                <Grid Grid.Column="2" Width="18" Height="64" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Ellipse x:Name="SelectedDot" Width="8" Height="8" Fill="{StaticResource PrimaryBrush}" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                    <StackPanel x:Name="PlayingBars" Orientation="Horizontal" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Top" Height="14">
                        <Rectangle Width="3" Height="8" RadiusX="1.5" RadiusY="1.5" Fill="{StaticResource PrimaryBrush}" Margin="0,6,2,0" />
                        <Rectangle Width="3" Height="14" RadiusX="1.5" RadiusY="1.5" Fill="{StaticResource PrimaryBrush}" Margin="0,0,2,0" />
                        <Rectangle Width="3" Height="10" RadiusX="1.5" RadiusY="1.5" Fill="{StaticResource PrimaryBrush}" Margin="0,4,0,0" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                <Setter TargetName="CardRoot" Property="Background" Value="{StaticResource SelectedTrackBrush}" />
                <Setter TargetName="CardRoot" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                <Setter TargetName="CardRoot" Property="BorderThickness" Value="2" />
                <Setter TargetName="SelectedDot" Property="Visibility" Value="Visible" />
            </DataTrigger>
            <DataTrigger Value="True">
                <DataTrigger.Binding>
                    <MultiBinding Converter="{StaticResource TrackIdentityMatchConverter}">
                        <Binding />
                        <Binding RelativeSource="{RelativeSource AncestorType=ListBox}" Path="DataContext.PlayingTrack" />
                    </MultiBinding>
                </DataTrigger.Binding>
                <Setter TargetName="CardRoot" Property="Background" Value="#402A2018" />
                <Setter TargetName="CardRoot" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                <Setter TargetName="CardRoot" Property="BorderThickness" Value="2" />
                <Setter TargetName="PlayingBars" Property="Visibility" Value="Visible" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <DataTemplate x:Key="HistoryItemTemplate">
        <Border Padding="12" CornerRadius="10" Background="#332A2A3F" BorderBrush="{StaticResource GoldBorderBrush}" BorderThickness="1" Margin="0,0,0,10" Cursor="Hand">
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick"
                              Command="{Binding DataContext.ReplayHistoryEntryCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                              CommandParameter="{Binding}" />
            </Border.InputBindings>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Width="48" Height="48" CornerRadius="8">
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding Track.CoverPath}" Stretch="UniformToFill" />
                    </Border.Background>
                </Border>
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="{Binding Track.Title}" Foreground="{StaticResource ForegroundBrush}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding Track.Artist}" Foreground="{StaticResource MutedForegroundBrush}" FontSize="12" TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding PlayedAt, StringFormat={}{0:HH:mm:ss}}" Foreground="{StaticResource PrimaryBrush}" FontSize="12" Margin="0,4,0,0" />
                </StackPanel>
            </Grid>
        </Border>
    </DataTemplate>

    <!-- Stats: Top — «Артист — Название» + «N ▶» справа золотым. -->
    <DataTemplate x:Key="StatsTopItemTemplate">
        <Grid Margin="6,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Track.Title}" Foreground="{StaticResource ForegroundBrush}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{Binding Track.Artist}" Foreground="{StaticResource MutedForegroundBrush}" FontSize="12" TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                <TextBlock Text="{Binding PlayCount}" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" />
                <TextBlock Text=" ▶" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" />
            </StackPanel>
        </Grid>
    </DataTemplate>

    <!-- Stats: Recent unique — «Артист — Название» + дата/время справа. -->
    <DataTemplate x:Key="StatsRecentItemTemplate">
        <Grid Margin="6,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Track.Title}" Foreground="{StaticResource ForegroundBrush}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{Binding Track.Artist}" Foreground="{StaticResource MutedForegroundBrush}" FontSize="12" TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <TextBlock Grid.Column="1"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       Foreground="{StaticResource MutedForegroundBrush}"
                       FontSize="12"
                       Text="{Binding PlayedAt, StringFormat={}{0:dd.MM HH:mm}}" />
        </Grid>
    </DataTemplate>

    <!-- Чип скрытого тега в попапе «+ N ещё ▾».
         DataContext каждого элемента = TagFilterItem.
         ToggleTagFilterCommand берётся с DataContext родительского ItemsControl,
         который code-behind выставляет в MainViewModel. -->
    <DataTemplate x:Key="HiddenTagChipTemplate">
        <Border Cursor="Hand" Margin="0,0,6,6">
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftClick"
                              Command="{Binding DataContext.ToggleTagFilterCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                              CommandParameter="{Binding}" />
            </Border.InputBindings>
            <Border.Style>
                <Style TargetType="Border" BasedOn="{StaticResource TagChipStyle}">
                    <Setter Property="Margin" Value="0,0,6,6" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter Property="Background" Value="#33D4A574" />
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding Tag.Name}" Style="{StaticResource TagChipTextStyle}" />
        </Border>
    </DataTemplate>

    <!-- Stats: Never played — только название и исполнитель. -->
    <DataTemplate x:Key="StatsNeverPlayedItemTemplate">
        <StackPanel Margin="6,8">
            <TextBlock Text="{Binding Title}" Foreground="{StaticResource ForegroundBrush}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
            <TextBlock Text="{Binding Artist}" Foreground="{StaticResource MutedForegroundBrush}" FontSize="12" TextTrimming="CharacterEllipsis" />
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>
