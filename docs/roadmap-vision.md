# MusicBakh — стратегический роадмап (2026 → 2029)

Документ фиксирует продуктовое видение MusicBakh, путь от текущего релиза `1.0.0` до версии `3.5.0`, а также «касты» идей, которые сознательно вынесены за пределы основного плана. Адресат — автор проекта; цель — иметь карту местности, чтобы в каждом цикле релизов опираться на единое видение, а не пересобирать приоритеты заново.

Документ описывает **направление**, а не контракт реализации. Конкретные имплементационные планы (`docs/superpowers/specs/...`) пишутся отдельно для каждого минора, опираются на этот файл и могут уточнять детали по факту.

> Сопутствующие документы:
> - [architecture.md](architecture.md) — фактическое устройство кода на момент релиза 1.0.0
> - [scope-deviations.md](scope-deviations.md) — отклонения от первоначального учебного скоупа
> - [release-checklist.md](release-checklist.md) — чек-лист публикации релизов

---

## 1. Ядро видения

> **MusicBakh = local-first music library + self-host cloud + smart discovery.**

Это якорь продукта. Каждая фича-кандидат проходит через один вопрос: усиливает ли она это ядро или тащит проект в платформенно-операционный хвост (модерация UGC, выплаты артистам, юридические риски hosted-стриминга, ML-инфраструктура big-tech уровня)? Если второе — фича уходит в касту «Возможно потом» или «Не делать соло».

Три равноправных столпа ядра:

1. **Local-first.** Пользователь владеет своими файлами, тегами, плейлистами и историей. Сервер — расширение, а не точка отказа. Десктоп остаётся первоклассным клиентом на всех фазах.
2. **Self-host cloud.** Когда у пользователя появляется потребность в синхронизации, веб-доступе и мобильнике, он поднимает свой сервер (Docker, VPS, домашний NAS) и владеет своим контуром. Hosted-тариф автора — только удобная альтернатива для тех, кто не хочет поднимать инфраструктуру.
3. **Smart discovery.** Рекомендации работают **поверх своей библиотеки** в первую очередь и поверх агрегированной анонимизированной информации сети self-host инстансов — во вторую. Это отличает MusicBakh от «личного Spotify» в позиционировании.

## 2. Версионирование

Используется semver:

- **Мажор** — смена парадигмы: `1.x` = десктоп, `2.x` = десктоп + backend, `3.x` = мультиклиентная социально-AI-обогащённая платформа.
- **Минор** — крупная фича-волна, обычно одна тема на минор (Audio Engine, Metadata, Backend MVP, и т.д.).
- **Патч (третья цифра)** — последовательные итерации внутри одного минора. Каждая итерация выпускается как самостоятельный релиз с инкрементом patch-цифры, чтобы пользователь получал обновления постепенно, а не ждал всей пачки фич минора несколько месяцев. Финальная стабилизация и публикация «готового» минора закрывает последнюю итерацию.

Пример (минор `1.1.0` = «Library 2.0», 8 итераций A–H):

- `1.0.1` — итерация A (чистые слои + SQLite).
- `1.0.2` — итерация B (FTS5 + extended history).
- `1.0.3` — итерация C (рейтинги, лайки, теги-ярлыки).
- ...
- `1.0.8` — итерация H (multi-column sort, кастомные колонки).
- `1.1.0` — стабилизация: финальный smoke, доводка перформанса, объявление «Library 2.0» готовой.

Аналогично для `1.2.0` = «Audio Engine 2.0» — `1.1.1` … `1.1.k` собирают итерации, потом `1.2.0` объявляется. И так далее по всем минорам фаз A/B/C.

Принципы:

- Каждая patch-итерация — рабочий релиз с инсталлятором: пользователь может остановиться на любой и продолжать пользоваться приложением.
- Когда `1.0.x` итерации закончились, минорный бамп до `1.1.0` происходит даже если нет новых фич — это сигнал «эпик закрыт».
- Архитектурные итерации (например, первая в каждом миноре) — допустимы и нужны: они закрывают техдолг, без которого следующая итерация рассыпается.
- Breaking changes публичного API между мажорами анонсируются за минор до выпуска мажора (через депрекацию в `2.x`-релизе перед `3.0.0`).

Условные таймлайны:

- Фаза A (`1.1.0` → `1.9.0`) — 9–12 месяцев соло.
- Фаза B (`2.0.0` → `2.8.0`) — 12–15 месяцев соло.
- Фаза C (`3.0.0` → `3.5.0`) — 10–12 месяцев соло.
- **До `3.5.0` — около 3 лет активной работы.**

Таймлайн ориентировочный, релизы не привязаны к календарю; цель — корректная последовательность, а не дедлайны.

## 3. Метки идей

В таблицах ниже каждая идея сопровождается тремя метками:

- **Усилия:** `S` = дни, `M` = 1–2 недели, `L` = месяц+, `XL` = многомесячный эпик.
- **Ценность:** `1`–`5` (5 = киллер-фича).
- **Риск:** 🟢 простой / 🟡 нужно подумать / 🔴 экспериментальный или с большим хвостом неизвестных.

Метки субъективные и пересматриваются при планировании конкретного минора.

---

## 4. Фаза A — Local-first Windows player (`1.1.0` → `1.9.0`)

Цель фазы: довести MusicBakh до уровня «foobar2000 / MusicBee, но с современным UX» и подготовить архитектуру для появления сервера в фазе B. На выходе из фазы — продукт, который имеет смысл сам по себе, даже если пользователь никогда не включит облачную часть.

### 4.1. `1.1.0` — Library 2.0

Заменяем JSON-индекс на полноценную базу и одновременно перестраиваем слои так, чтобы дальнейшие миноры не упирались в `MainWindow.xaml.cs` как точку сборки.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| SQLite + EF Core/Dapper, миграции схемы | M | 5 | 🟡 |
| Чёткие слои: `MusicBakh.Core` (Domain), `MusicBakh.Application` (use-cases), `MusicBakh.Infrastructure` (репозитории, файловая система, API), `MusicBakh.Presentation.Wpf` | L | 5 | 🟡 |
| FTS5-поиск по тегам, имени, альбому, исполнителю, лирике | S | 5 | 🟢 |
| Умные плейлисты по правилам (`жанр=рок И год≥2010 И rating≥4`) | M | 5 | 🟢 |
| Статические плейлисты с drag-and-drop, импорт/экспорт `.m3u8`/`.pls` | S | 4 | 🟢 |
| Рейтинги 0–5★ и лайки/дизлайки | S | 4 | 🟢 |
| Пользовательские теги-ярлыки (mood, situation: «утро», «работа», «тренировка») | S | 4 | 🟢 |
| Альбомный и исполнительский view (группировка) | M | 5 | 🟢 |
| Сортировка по нескольким колонкам, кастомные колонки, пресеты | S | 3 | 🟢 |
| Расширенная история прослушиваний (полный журнал, агрегации: топ-50, last played, never played) | S | 4 | 🟢 |
| Watched folders — авто-сканирование каталогов с FileSystemWatcher | M | 5 | 🟡 |

**Архитектурное требование:** SQLite заводится **сразу через репозитории и Unit of Work**, не «поверх» JSON-кода. Презентация знает только об интерфейсах `ITrackRepository`, `IPlaylistRepository`, `IListeningHistoryRepository`, `ILibraryQueryService`. Это закладывает фундамент под фазу B: репозитории можно будет переключить на удалённый источник без переписывания view-models.

**Definition of done:**
- Миграция существующих пользовательских данных (`userTracks.json`) в SQLite автоматически при первом запуске, с резервной копией старого файла.
- Бенчмарк: библиотека из 50 000 треков открывается за < 2 секунд, поиск даёт первые результаты за < 100 мс.
- Покрытие репозиториев интеграционными тестами на in-memory SQLite.

### 4.2. `1.2.0` — Audio Engine 2.0

Расширяем поддержку форматов и закрываем все «привычные» фичи плеера. Аудиофильские крайности (ASIO, DSD, bit-perfect) сознательно вынесены в касту «Возможно потом» — они дают мало пользы массовому слушателю и съедают много времени.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Поддержка FLAC, OGG Vorbis, Opus, AAC/M4A, ALAC, AIFF, WMA через ManagedBass или NAudio + расширения | M | 5 | 🟡 |
| Gapless playback (треки альбома без промежутков) | M | 5 | 🟡 |
| Crossfade с настраиваемой длительностью | S | 4 | 🟢 |
| 10-полосный эквалайзер с пресетами и поддержкой пользовательских профилей | M | 4 | 🟢 |
| ReplayGain (чтение тегов, fallback на анализ) | M | 4 | 🟡 |
| DSP-цепочка: bass boost, mono mix, нормализация пиков | M | 3 | 🟢 |
| Sleep timer (через N минут / в конце трека / альбома) | S | 3 | 🟢 |
| Будильник (запуск воспроизведения по расписанию) | S | 2 | 🟢 |
| Скорость воспроизведения 0.5×–2× с сохранением тональности | S | 3 | 🟢 |
| A-B repeat фрагмента | S | 2 | 🟢 |

**Definition of done:**
- Все перечисленные форматы проигрываются, теги читаются и пишутся.
- Gapless проверен на референсных альбомах (Pink Floyd «The Dark Side of the Moon», DJ-миксы).
- ReplayGain корректно нормализует громкость при переключении между «громкими» и «тихими» альбомами без вмешательства пользователя.

### 4.3. `1.3.0` — Metadata & Lyrics

Превращаем плеер в полноценный теггер и подключаем лирику. Все внешние источники прячутся за стабильными provider-интерфейсами — это первый шаг к provider-архитектуре `1.7.0`.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Tag editor (single + batch), запись тегов в файл через TagLib# | M | 5 | 🟢 |
| Discogs API: релиз-инфа, лейбл, состав исполнителей, страна | M | 4 | 🟢 |
| Last.fm: биография исполнителя, похожие артисты | S | 4 | 🟢 |
| Lyrics-провайдеры: Musixmatch / LRCLib / Genius, поддержка `.lrc` синхронизированных | M | 5 | 🟢 |
| Karaoke-mode (подсветка строк по таймкодам) | M | 3 | 🟢 |
| AcoustID/Chromaprint: распознавание трека по акустическому отпечатку | M | 4 | 🟡 |
| AcousticBrainz/Essentia: BPM, тональность, danceability | M | 3 | 🟡 |
| Детектор дубликатов: по хешу + нечёткий матчинг метаданных | M | 4 | 🟡 |
| Library health: отчёт «битые файлы», «нет обложки», «нет жанра», батч-фикс | S | 4 | 🟢 |
| Импорт из других плееров (iTunes XML, foobar2000 playlists, Winamp) | M | 2 | 🟢 |

**Архитектурное требование:** все внешние интеграции скрыты за интерфейсами `IMetadataProvider`, `ILyricsProvider`, `IAudioFingerprintService`, `IArtistInfoProvider`. Это не plugin loading: реализации лежат в `MusicBakh.Infrastructure` и собираются в DI-контейнере. Но **пользователь будущего плагин-API уже сможет подключить свой источник** без правки доменной логики.

### 4.4. `1.4.0` — UX polish

Закрываем разрыв «работает, но выглядит как студенческий проект». Эта версия — самая высокозаметная для пользователя; параллельно вкладываемся в локализацию, чтобы потом не платить долг.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Тёмная / светлая / системная тема + dynamic palette из обложки текущего трека | M | 5 | 🟢 |
| Mini-player (компактное окно поверх остальных) | S | 4 | 🟢 |
| Now-Playing OSD-уведомление при смене трека (Windows toast или своё) | S | 4 | 🟢 |
| Tray-иконка с быстрым управлением + Windows jump list | S | 4 | 🟢 |
| Глобальные хоткеи (вне окна), включая медиа-клавиши и наушниковые кнопки | S | 4 | 🟢 |
| Drag-and-drop файлов/папок прямо в окно для импорта | S | 5 | 🟢 |
| i18n-инфраструктура (resx), RU/EN на старте, заготовка под украинский/казахский/немецкий/испанский | M | 4 | 🟢 |
| Полноэкранный «Now Playing» с большой обложкой, спектром, синхронизированной лирикой | M | 4 | 🟢 |
| Визуализатор: спектр, осциллограф, реактивные градиенты | M | 3 | 🟡 |
| Touch-friendly mode (увеличенные кнопки) для планшетов 2-в-1 | M | 2 | 🟢 |
| Адаптивная вёрстка под малые/большие окна | M | 3 | 🟢 |
| Кастомные горячие клавиши (редактор в настройках) | S | 3 | 🟢 |

### 4.5. `1.5.0` — Stats + Scrobbling + Discord Rich Presence

Первый «не одиночный» минор фазы A: интеграции с внешними сервисами, которые не требуют собственного backend.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Дашборд: тепловая карта прослушиваний по часам/дням, top-исполнители за период, тренды | M | 4 | 🟢 |
| «Год в музыке» — Spotify Wrapped-аналог в декабре, экспорт в PNG/Story-формат | M | 4 | 🟢 |
| Streaks (слушал каждый день N дней), badges, milestone-уведомления | S | 2 | 🟢 |
| Last.fm scrobbling с офлайн-очередью | S | 4 | 🟢 |
| ListenBrainz scrobbling | S | 3 | 🟢 |
| Discord Rich Presence («слушает X — Y, обложка, прогресс») | S | 4 | 🟢 |
| Экспорт статистики в CSV/JSON | S | 2 | 🟢 |
| Сравнение «текущий vs прошлый период» | S | 2 | 🟢 |

### 4.6. `1.6.0` — Podcasts + Internet Radio

Расширяет понятие «трек» до «единицы прослушивания»: подкаст-эпизод (с прогрессом) и радиопоток (без длительности). Эта версия впервые ломает предположение «всё имеет файл и теги» — её надо делать после `1.1.0` библиотеки.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| RSS-фиды подкастов: подписка, авто-загрузка эпизодов, сохранение прогресса | M | 3 | 🟢 |
| Каталог интернет-радио через radio-browser.info, сохранение станций в библиотеку | S | 3 | 🟢 |
| Запись радиопотока в файл | S | 2 | 🟢 |
| «Личное радио» — бесконечный плейлист на основе текущего трека и истории | M | 4 | 🟡 |
| Songkick / Bandsintown: концерты исполнителей рядом | M | 3 | 🟢 |
| Кнопки «послушать на Bandcamp/Beatport/Apple Music/Spotify/Яндекс.Музыке» | S | 3 | 🟢 |
| UPnP/DLNA рендеринг — играть на колонке/телевизоре в локальной сети | M | 3 | 🟡 |

### 4.7. `1.7.0` — Internal Provider Architecture

**Это не plugin loading.** Мы не загружаем внешние DLL, не делаем marketplace и не возимся с supply-chain безопасностью. Мы лишь формализуем уже существующие к этому моменту интерфейсы (`IMetadataProvider`, `ILyricsProvider`, `IScrobbler`, `IVisualizer`, `IImportSource`, `IAudioDecoder`) и проектируем модульную внутреннюю структуру: реализации лежат в своих сборках, собираются в DI, могут включаться/выключаться пользователем в настройках.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Стабильные внутренние контракты (NuGet `MusicBakh.Contracts` для будущей экосистемы) | M | 4 | 🟢 |
| Отдельные сборки `Providers.Lastfm`, `Providers.Discogs`, `Providers.Musixmatch`, `Providers.AcoustId` | M | 4 | 🟢 |
| Менеджер модулей (UI: вкл/выкл, версии, статус подключения, токены API) | S | 3 | 🟢 |
| Тесты контрактов: каждая реализация прогоняется через общий набор тестов поведения | M | 4 | 🟡 |

Почему важно не делать сразу настоящие плагины: контракты к этому моменту меняются каждый минор. Если выпустить публичный API раньше, мы либо ломаем сторонних разработчиков, либо застываем на сыром API. Реальный plugin loading появится после фазы C, когда контракты стабилизируются 1–2 года.

### 4.8. `1.8.0` — Installer + Updater + Crash logs

Перевод проекта из «exe в релизах GitHub» в «нормально установленное приложение Windows».

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Авто-апдейтер (Velopack — современная альтернатива Squirrel.Windows) | M | 5 | 🟡 |
| Authenticode-подпись инсталлятора (избавляет от SmartScreen-предупреждения) | S | 4 | 🟡 |
| Структурированное логирование (Serilog) + ротация логов | S | 3 | 🟢 |
| Локальный crash dump в `%LocalAppData%` + опциональная отправка через Sentry/собственный endpoint | S | 3 | 🟢 |
| Delta-обновления (только изменённые файлы) | M | 3 | 🟡 |
| Канал «beta» и «stable» в авто-апдейтере | S | 2 | 🟢 |

### 4.9. `1.9.0` — Architecture hardening

Архитектурный минор-мост в фазу B. Без него `2.0.0` будет строиться на песке.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| DI-контейнер (`Microsoft.Extensions.DependencyInjection`) — собирается не в `MainWindow.xaml.cs` | M | 5 | 🟢 |
| Полный переезд на CommunityToolkit.Mvvm (source generators, ObservableProperty, RelayCommand) | M | 4 | 🟢 |
| MediatR-стиль pipeline для команд (Play, Import, Tag-edit) — упростит логирование, undo/redo, телеметрию | M | 4 | 🟢 |
| Async-первый дизайн: уход от блокирующих вызовов в UI-потоке | M | 4 | 🟢 |
| UI-тесты через FlaUI или WinAppDriver на ключевые сценарии (импорт, поиск, плейлист, воспроизведение) | M | 3 | 🟡 |
| GitHub Actions: матрица `dotnet test`, code coverage, авто-сборка инсталлятора при теге `v*` | S | 4 | 🟢 |
| Code analysis: Roslyn analyzers, treat warnings as errors, EditorConfig, `.editorconfig.allowed_violations.md` | S | 3 | 🟢 |
| Performance budget: бенчмарки запуска, открытия библиотеки, переключения треков как часть CI | M | 3 | 🟡 |

**Definition of done фазы A:**
- Релиз `1.9.0` запускается на чистой Windows 10 1809 без предупреждений SmartScreen.
- Tag-редактор, умные плейлисты, lyrics и watched folders работают без падений на коллекции 100k+ треков.
- Архитектура готова к подключению удалённого репозитория без переписывания view-models.
- CI гонит тесты и автоматически собирает инсталлятор.

---

## 5. Фаза B — Backend + companions (`2.0.0` → `2.8.0`)

Цель фазы: вокруг десктопа появляется опциональный backend, который хранит сетевую часть жизни пользователя (синхронизация, web-доступ, мобильный компаньон, рекомендации) и при этом остаётся self-host'ом первого класса. Hosted-тариф автора — приятное удобство, а не точка отказа.

### 5.1. `2.0.0` — Backend MVP

Минимально достаточный сервер, который имеет смысл выкатить.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| ASP.NET Core (.NET 10) + PostgreSQL | XL | 5 | 🟡 |
| Аутентификация: email + пароль, refresh-токены (JWT), один OAuth-провайдер на старте (Google) | L | 5 | 🟡 |
| Users / devices / sessions с управлением через UI клиента | M | 4 | 🟢 |
| Self-host: Docker-образ, `docker-compose.yml`, инструкция установки на VPS и NAS (Synology, TrueNAS, Unraid) | M | 5 | 🟢 |
| Публичный REST API, версионируемый (`/v1/...`), OpenAPI-схема, генерируемый TS/C# SDK | M | 4 | 🟢 |
| Rate limiting (per-user, per-endpoint) | S | 3 | 🟢 |
| Базовый audit log (вход, смена пароля, выдача токенов) | S | 3 | 🟢 |
| Простая admin-консоль для self-host-владельца (список пользователей, инвайты, размер хранилища) | M | 3 | 🟢 |
| Документация на `docs.musicbakh.app` (Docusaurus или mkdocs) | M | 3 | 🟢 |

**Сознательно не делаем в 2.0:** Duende/OpenIddict со всеми OAuth-провайдерами сразу, magic links, gRPC параллельно REST, Redis (пока нет нагрузки, которая того стоит), hosted billing, полный GDPR/152-ФЗ compliance, мульти-тенант. Всё это — поздние оптимизации, которые в MVP сожгут квартал работы.

### 5.2. `2.1.0` — Sync Engine v1

Главный технический риск фазы. Делаем «достаточно хороший» sync, не идеальный.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Server revision per user (монотонный счётчик ревизий на пользователя) | M | 5 | 🟡 |
| Client change log с idempotency keys для отправки изменений | M | 5 | 🟡 |
| Last-write-wins per field (с server timestamp, не client) | M | 4 | 🟡 |
| Soft delete (tombstones) для треков, плейлистов, тегов | S | 4 | 🟢 |
| Sync cursor: клиент скачивает только изменения с последней ревизии | M | 5 | 🟢 |
| Конфликт-таблица + ручной разрешитель в UI (для редких случаев типа изменения одного и того же поля с двух устройств одновременно) | M | 4 | 🟡 |
| Sync объектов: треки (метаданные), плейлисты, рейтинги, теги-ярлыки, прогресс прослушивания, история | L | 5 | 🟡 |
| Офлайн-очередь изменений с retry и backoff | M | 5 | 🟡 |
| Передача fingerprints, а не файлов: сервер хранит AcoustID-хеши, файл остаётся локально | M | 4 | 🟡 |
| Тест-сьют: chaos-сценарии (рандомные дисконнекты, частичные апдейты, гонки) | M | 3 | 🟡 |

**Сознательно не делаем в 2.1:** CRDT во всём (точечный CRDT появится в `3.0.0` под collaborative playlists), event sourcing, E2E-шифрование метаданных (см. касту «Возможно потом»), real-time sync через WebSockets для всех объектов сразу.

### 5.3. `2.2.0` — Self-host Cloud Library

Файлы пользователя оказываются на его сервере. **Hosted-режим (мы храним чужую музыку) сознательно не делается** — это юридически и финансово неподъёмно соло.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Upload пользовательских файлов на свой сервер | L | 5 | 🟡 |
| S3-совместимое хранилище (MinIO, Backblaze, Yandex Object Storage, AWS S3) | M | 4 | 🟡 |
| Транскодирование в Opus/AAC для быстрой раздачи мобильным клиентам, оригинал хранится для десктопа | M | 4 | 🟡 |
| HLS/DASH-стриминг с server-side seek, адаптивным битрейтом | L | 5 | 🟡 |
| Подписанные URL с коротким TTL, защита от хотлинков | S | 3 | 🟢 |
| WebDAV/SMB/FTP/S3 как источники библиотеки (для NAS-пользователей — Synology, TrueNAS, Unraid) | L | 4 | 🟡 |
| Инкрементальный бэкап локальной библиотеки в облако | M | 4 | 🟢 |
| Восстановление библиотеки на чистую машину из бэкапа | S | 4 | 🟢 |

**Сознательно не делаем:** hosted music storage (мы хранили бы чужую музыку → DMCA, RIAA-style жалобы, юр. вопросы), дедупликация по acoustic fingerprint между разными пользователями (та же юридическая зона), family sharing с публичным шарингом полных треков, открытые публичные ссылки на полные треки (превью 30 секунд — да, полный трек — нет).

### 5.4. `2.3.0` — Web PWA companion

Веб-клиент как **дополнение** к десктопу/мобилке, не как полноценная замена. PWA, потому что закрывает Linux/macOS, мобильный браузер и embeddable-player одной кодовой базой.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Выбор стека: Blazor WebAssembly (переиспользует C#) или Next.js + TypeScript (богаче экосистема) — решение фиксируется в дизайне 2.3 | XL | 5 | 🟡 |
| Library UI: списки, фильтры, поиск | L | 5 | 🟢 |
| HTML5 Audio + Media Session API (управление с медиа-клавиш, lock screen на телефоне) | S | 4 | 🟢 |
| PWA: оффлайн-режим (метаданные), установка как приложение, push-уведомления | M | 4 | 🟢 |
| Share-links с metadata-only preview (обложка + 30 секунд аудио) | S | 4 | 🟢 |
| Embeddable player через iframe (для блогов, форумов, личных сайтов) | S | 2 | 🟢 |
| Web-доступ к статистике и Year-in-Music | S | 3 | 🟢 |

**Сознательно не делаем:** полноценный Web Audio DSP-стек (EQ, crossfade) — десктоп остаётся основным клиентом для этих сценариев, Cast/AirPlay из веба, скачивание оригиналов через веб (только стрим).

### 5.5. `2.4.0` — Subsonic-compatible API

Поднято высоко в фазе B по простой причине: **реализация Subsonic API даёт «всю чужую мобильную экосистему бесплатно»**. Существующие клиенты (DSub, play:Sub, Symfonium, Ultrasonic, substreamer, и десятки других на iOS/Android/Linux/macOS/web) начнут работать с MusicBakh без написания собственной мобилки.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Реализация Subsonic API endpoints (`getMusicFolders`, `getArtists`, `stream`, `download`, `scrobble`, `createPlaylist`, etc.) | L | 5 | 🟢 |
| Совместимость с топ-5 клиентами (DSub, Symfonium, play:Sub, Ultrasonic, substreamer): по факту работы, не на словах | M | 5 | 🟡 |
| Расширения API для специфических MusicBakh-фич (теги-ярлыки, умные плейлисты), оформленные как опциональные | S | 3 | 🟢 |
| Документация для сторонних разработчиков: «как использовать MusicBakh как Subsonic-бэкенд» | S | 4 | 🟢 |

Стратегически это снижает приоритет собственной мобилки: можно собрать первые пользовательские истории, прежде чем вкладываться в `2.5`.

### 5.6. `2.5.0` — Mobile companion

Собственное мобильное приложение, но **компаньон, не полноценный плеер**. Технологический стек — `MAUI` (переиспользует C#-логику Core) или Flutter (богаче UI, своя экосистема). Решение в дизайне минора, склонность — к MAUI.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Login + browse библиотеки | M | 5 | 🟡 |
| Remote control десктопа/сервера: «передать на это устройство», управление очередью | M | 4 | 🟡 |
| Прогресс воспроизведения, синхронизируется в реальном времени | S | 4 | 🟢 |
| Воспроизведение в фоне, lock-screen controls, медиа-уведомление | M | 5 | 🟡 |
| Базовый стрим с self-host (HLS) | M | 5 | 🟡 |
| Оффлайн-кеш метаданных (без скачивания файлов в первой версии) | S | 4 | 🟢 |
| Импорт треков с устройства (загрузка локальных mp3 в свой self-host) | M | 3 | 🟢 |

**Сознательно не делаем в 2.5:** Android Auto, CarPlay, Wear OS / watchOS, полный оффлайн-download плейлистов (это `2.x.x` после того, как мобилка устоится), parity по EQ/crossfade с десктопом, биометрический login. Всё это — отдельные миноры за `2.5`.

### 5.7. `2.6.0` — Recommendations v1

Простые методы, которые работают без big-data:

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Content-based: похожие треки по BPM, тональности, жанру, тегам, AcousticBrainz-фичам | M | 4 | 🟡 |
| Last.fm similar artists как источник | S | 4 | 🟢 |
| Daily Mix — ежедневные плейлисты по жанрам/настроениям из своей библиотеки | M | 5 | 🟢 |
| Discover Weekly-аналог: связанные артисты + редко слушаемое в своей библиотеке | M | 5 | 🟢 |
| Personal Radio — бесконечная очередь, обучается на лайках и скипах текущей сессии | M | 5 | 🟡 |
| Onboarding: «выбери 5 любимых исполнителей» → bootstrap-плейлист для пустой библиотеки | S | 3 | 🟢 |

**Сознательно не делаем:** collaborative filtering между пользователями (нет базы), тяжёлый ML pipeline, обучение моделей на сервере.

### 5.8. `2.7.0` — Scrobble + соц-lite

Социальные функции вводятся минимальным набором: пользователь имеет публичный профиль и может следить за другими, но **без** комментариев, групп, DM и модерации UGC.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Свой scrobbling-эндпоинт + совместимость с Last.fm API (сторонние клиенты могут слать MusicBakh как Last.fm-сервер) | M | 4 | 🟢 |
| Публичный профиль: топ-треки, исполнители, обложка-коллаж, текущий статус | S | 4 | 🟢 |
| Follow / followers (без ленты, как RSS-подписка) | S | 3 | 🟢 |
| Privacy settings: публичный / только для друзей / приватный профиль | S | 3 | 🟢 |
| Импорт scrobbles из Last.fm / ListenBrainz / Spotify-экспорта | M | 4 | 🟢 |

**Сознательно не делаем:** комменты, группы, DM, ActivityPub-федерация, репорты, бан-система, шейдоу-бан, антифрод-боты. Всё это превращает плеер в социальную платформу с операционным хвостом, который соло-разработчик не утянет.

### 5.9. `2.8.0` — Spotify Connect-style transfer

Передача воспроизведения между **своими** устройствами. Это **не** multi-room: то и другое смешивать нельзя, multi-room — отдельный инженерный зверь с компенсацией latency.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| «Передать на устройство» — продолжить прослушивание на другом своём клиенте | L | 5 | 🟡 |
| Синхронизирующаяся очередь через SignalR / WebSockets | M | 5 | 🟡 |
| «Играть следующим» vs «в конец очереди» | S | 4 | 🟢 |
| Состояние плеера (позиция, громкость, режим повтора) синхронизируется с задержкой ≤ 1 сек | M | 4 | 🟡 |

**Definition of done фазы B:**
- Self-host инстанс разворачивается одной командой `docker-compose up`.
- Десктоп, web PWA и хотя бы один Subsonic-клиент работают с одним аккаунтом, видят одинаковую библиотеку и плейлисты.
- Sync переживает 24-часовой офлайн без потери данных.
- Recommendations v1 даёт качественный Daily Mix на коллекциях ≥ 1000 треков.

---

## 6. Фаза C — Social-lite + AI + Reco v2 (`3.0.0` → `3.5.0`)

Цель фазы: добавить социальные элементы и AI-обогащение, оставаясь в рамках того, что соло-разработчик может поддерживать без модерационной команды. AI используется как **усилитель** базовых сценариев, а не как самостоятельный gimmick (никаких TTS-DJ, никаких AI voice clones).

### 6.1. `3.0.0` — Social-lite

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Лента активности друзей (что слушают, что лайкнули, какие плейлисты собрали) | M | 5 | 🟢 |
| Collaborative playlists — несколько пользователей правят один плейлист, **здесь точечно вводится CRDT** для разрешения конфликтов | M | 5 | 🟡 |
| Музыкальная совместимость с другом (% пересечения вкусов) с красивой визуализацией | S | 3 | 🟢 |
| Year-in-Music как вирусный шеринг-объект (шаблоны под Stories) | S | 4 | 🟢 |
| Privacy-первый дизайн: всё опционально, всё отключаемо | S | 4 | 🟢 |

**Сознательно не делаем:** комменты, группы, DM, модерация UGC, бан-система, шейдоу-бан. Все эти фичи требуют отдельного направления работы (модерация, антифрод, юр), которое соло-разработчик не утянет.

### 6.2. `3.1.0` — Listening Rooms v1

Совместное прослушивание в стиле Watch2Gether-для-музыки, но **намеренно простое**.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Комната с host'ом, который управляет очередью | M | 5 | 🟡 |
| Клиенты подтягивают позицию приблизительно (без latency-compensation) | M | 4 | 🟡 |
| Чат внутри комнаты (текстовый, отдельно от аудио-стрима) | M | 4 | 🟢 |
| Реакции на треки в реальном времени (эмодзи) | S | 3 | 🟢 |
| Публичные комнаты-«радиостанции» с пользователем-«DJ» | M | 4 | 🟡 |

**Сознательно не делаем:** WebRTC voice chat, точную синхронизацию sub-100ms, межстрановую low-latency синхронизацию. Это R&D-направление, не фича.

### 6.3. `3.2.0` — AI Assistant over Library

Ставка на **полезный** AI — над библиотекой пользователя, а не как gimmick.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Промпт-плейлисты: «собери мне 30 треков для долгой ночной поездки по трассе» → LLM матчит на твою библиотеку | M | 5 | 🟡 |
| «Объясни этот трек» — LLM пишет краткий контекст про артиста, эру, влияние, что слушать после | S | 4 | 🟢 |
| AI mood/situation теги: после прослушивания LLM предлагает теги (можно принять/отклонить) | M | 3 | 🟡 |
| AI-описание исполнителя на твоём языке, если в источниках только английский | S | 3 | 🟢 |
| Голосовые команды: Whisper локально + intent-классификатор для команд «поставь что-нибудь спокойное», «следующий», «лайк» | L | 4 | 🟡 |
| Локальный LLM-агент (опция для тех, кто не хочет слать запросы вовне): Ollama-совместимый интерфейс, llama.cpp под капотом | L | 3 | 🟡 |

**Сознательно не делаем:** AI DJ с TTS-репликами между треками (gimmick), auto-mashup (юр.+качество), AI-кавер голосами артистов (этика+юр.минное поле), AI-генерация обложек для всех треков (низкая ценность). Stems separation (Demucs/Spleeter) — только как локальная экспериментальная фича для караоке-мода, не как самостоятельная фича.

### 6.4. `3.3.0` — Recommendations v2 (embeddings)

Вторая версия рекомендаций, уже на эмбеддингах, но **без** big-tech-усложнений.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Audio embeddings: CLAP / MERT / OpenL3, треки → вектор 512–1024-d | L | 5 | 🟡 |
| pgvector или Qdrant как векторное хранилище | M | 4 | 🟢 |
| Similar tracks по cosine similarity, доступно в контекстном меню любого трека | S | 4 | 🟢 |
| User embedding как взвешенное среднее истории прослушиваний | M | 4 | 🟡 |
| Объяснимые рекомендации: «рекомендую X, потому что в твоей библиотеке есть Y и Z с похожим звучанием/настроением» | M | 3 | 🟡 |
| Простой reranker (LightGBM/XGBoost) на «доскипает / лайкнет» с пользовательской историей | L | 4 | 🟡 |
| Cold-start: новые пользователи получают рекомендации по своему первому импорту через audio-fingerprint match | M | 4 | 🟢 |

**Сознательно не делаем:** two-tower retrieval (нужна big-tech база пользователей, иначе игрушка), multi-armed bandits на маленьком трафике, обучение моделей с нуля. Используем pre-trained эмбеддинги.

### 6.5. `3.4.0` — Public API + интеграции

Открываем платформу для third-party интеграций — но **без plugin marketplace** (он остаётся в касте «Возможно потом» из-за supply-chain рисков).

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Public API: OAuth 2.0 + PKCE, scopes (read library, write playlists, read history, write scrobbles) | M | 5 | 🟢 |
| Webhooks: «трек начался», «лайкнул», «плейлист создан» — для IFTTT/Zapier-like сценариев | S | 3 | 🟢 |
| Home Assistant component (нативная интеграция) | M | 3 | 🟢 |
| Telegram-/Discord-боты: команды, скробблинг в канал, поиск по библиотеке | M | 3 | 🟢 |
| Расширение Subsonic API в сторону MusicBakh-специфичных фич | S | 3 | 🟢 |
| Developer docs + SDK на C# и Python (TypeScript добавляется позже) | M | 4 | 🟢 |

### 6.6. `3.5.0` — Creative-lite + геймификация

Лёгкие фичи, которые делают приложение «живым», без превращения его в DJ-софт или караоке-платформу.

| Идея | Усилия | Ценность | Риск |
|---|---|---|---|
| Lyrics-карточки: красивая обложка с цитатой из лирики — экспорт в PNG для соцсетей | S | 3 | 🟢 |
| Recap-изображения/короткие видео (как Spotify Canvas) — обложка + спектр + лирика | M | 4 | 🟡 |
| «Угадай трек» по 5 секундам (Heardle-mode) на своей библиотеке или общей | M | 3 | 🟢 |
| Викторины на знание исполнителя/жанра, leaderboard | M | 2 | 🟢 |
| Достижения за разнообразие («послушал треки из 30 стран», «50 жанров») | S | 2 | 🟢 |
| Headphone AutoEQ presets (база AutoEQ.app, автоматически по подключённой модели) | S | 4 | 🟢 |
| CD ripping с AccurateRip (для энтузиастов с CD-приводами) | M | 2 | 🟢 |
| Музыкальная генеалогия: визуальное древо «жанр X произошёл от Y», твой путь по нему | M | 3 | 🟡 |

**Definition of done фазы C:**
- Пользователь может попросить «собери мне плейлист для X» голосом или текстом и получить осмысленный результат на своей библиотеке.
- Друзья могут совместно собирать плейлисты без конфликтов и потерь.
- Public API имеет хотя бы одну работающую third-party интеграцию (Home Assistant или Telegram-бот).
- Reco v2 даёт качественные «similar tracks» на любых треках, не только популярных.

---

## 7. Каста «Возможно потом» (за пределами `3.5.0`)

Идеи, которые имеет смысл реализовать, но только после того, как ядро стабилизируется и появятся ресурсы (сообщество, контрибуторы, возможно — спонсоринг).

### Архитектурные

- **Plugin loading с marketplace.** Загрузка сторонних DLL с sandbox-изоляцией (AssemblyLoadContext), supply-chain security (sigstore-подписи), code review, версионирование API, каталог, отзывы. После 3.5, когда внутренний provider API стабилизируется минимум на год.
- **Avalonia-миграция** или нативный Linux/macOS клиент. Web PWA до этого момента закрывает большую часть запроса.
- **E2E-encrypted private mode** — опциональный профиль, где метаданные пользователя зашифрованы клиентским ключом, сервер видит только зашифрованные blob'ы. Жертвуем рекомендациями и публичными профилями ради приватности.
- **Real-time multi-room sync** уровня Sonos с latency-compensation между устройствами.

### Аудио и интеграции

- **ASIO / WASAPI Exclusive / DSD / 24/192 Hi-Res / MQA** — если в комьюнити появится спрос аудиофилов. MQA сейчас почти мёртвый, скорее не стоит.
- **Tesla / автомобильные браузерные UI**, Wear OS / watchOS компаньоны.
- **Stems separation** (Demucs/Spleeter) как пользовательская фича для караоке/DJ-сценариев.
- **Chromecast / AirPlay** receivers (через сторонние библиотеки).
- **CD/Vinyl-режимы** в полном объёме: vinyl assistant с распознаванием через микрофон, поддержка USB-DAC с native ASIO.

### Рекомендации и социалка

- **Two-tower retrieval / multi-armed bandits** — когда наберётся реальная база пользователей.
- **ActivityPub-федерация** — R&D-трек, требует решения вопросов модерации между инстансами.
- **Listening Rooms v2** с voice chat (WebRTC) и точным latency-compensation.
- **Time-machine воспроизведение** топ-100 на дату N лет назад (требует исторических чартов).
- **Family sharing** — несколько членов семьи, разные библиотеки, общая подписка.

### Бизнес и экосистема

- **Hosted-тариф автора** с биллингом, лимитами, поддержкой.
- **Sponsor partnerships** с независимыми артистами и лейблами.
- **Localization to 15+ языков** с участием сообщества (Crowdin / Weblate).

---

## 8. Каста «Не делать соло, точка»

Десять направлений, в которые нельзя уходить даже с AI-агентами, потому что они требуют команды, денег, юристов, модерации, SLA, договоров — а не только написанного кода. Эта каста — карта реальности, чтобы при контрибутор-предложениях «давай добавим X» сразу было видно, что X — это другая компания.

1. **Полноценная социальная сеть вокруг музыки.** Комменты, группы, DM, модерация UGC, антифрод, репорт-система, шейдоу-бан, бан-волны — это операционная нагрузка большой команды.
2. **Hosted public music storage с публичным шарингом полных треков.** DMCA, RIAA-аналоги, юридические риски хранения чужой музыки.
3. **Plugin marketplace с безопасной модерацией стороннего кода.** Supply-chain security — отдельная инженерная специальность.
4. **Artist platform с верификацией, выплатами и контрактами.** Платёжные системы, налоги, антифрод, договоры, аудит. Отдельный бизнес.
5. **Federation как стабильная и поддерживаемая фича.** Совместимость с Mastodon/Pleroma/Misskey, модерация между инстансами, миграция аккаунтов — большой долгий хвост.
6. **Multi-room low-latency playback уровня Sonos.** Latency-compensation между разными устройствами/ОС/наушниками — R&D-направление.
7. **Spotify-уровень рекомендаций.** Production ML-инфраструктура big-tech-уровня требует команды и базы пользователей.
8. **Полный mobile parity на iOS+Android+Auto+CarPlay+Watches.** Это четыре–пять продуктов одновременно с разными платформенными политиками.
9. **Глобальная hosted streaming инфраструктура для бесплатных пользователей.** Storage, CDN, traffic costs, support — деньги и DevOps.
10. **Legal-safe SoundCloud/Spotify-like публичный шаринг чужой музыки.** Юридическая модель индустрии лицензирования.

---

## 9. Каста «Не делать никогда»

Эти идеи будут возникать (от себя или контрибуторов) — заранее зафиксированный отказ экономит часы обсуждений.

- **NFT / Web3 wallet login / blockchain-привязка покупок.** Рынок мёртв, аудитория токсична, ценность отсутствует.
- **AI voice cloning артистов** («спой как X»). Этическое и юридическое минное поле, репутационный риск.
- **MQA-сертификация.** Технология практически мертва после ухода Tidal на FLAC.
- **VR-клуб** (Quest/SteamVR) с «летающими обложками» — игрушка, не продукт.
- **Audio-watermarking** приватных загрузок. Ломает совместимость, ловится тривиально, бесполезно.
- **Spotify Connect** через reverse-engineered librespot. Юр.минное поле, может прилететь от Spotify.
- **Brain-computer interface** для смены треков «силой мысли». :)

---

## 10. Сквозные треки

Помимо тематических миноров, есть направления, которые **вшиваются в каждый минор** и не имеют отдельной версии:

### 10.1. Безопасность

- Регулярный аудит зависимостей (Dependabot, GitHub Security Advisories).
- Авторизация и ввод данных проверяются на каждом эндпоинте после `2.0`.
- Шифрование секретов в локальном хранилище десктопа (DPAPI на Windows, keychain на macOS/Linux).
- 2FA (TOTP) и passkeys (WebAuthn) — целятся в минор после `2.0`, не привязаны жёстко.

### 10.2. Производительность

- Бенчмарк-сьют как часть CI: запуск приложения, открытие библиотеки 100k треков, поиск, переключение треков. Перфоманс-бюджет фиксируется в `1.9.0`.
- Профилирование memory/CPU на каждом миноре с большой UI-нагрузкой (`1.4.0`, `1.6.0`).

### 10.3. Наблюдаемость

- Структурированное логирование (Serilog) с `1.8.0`.
- OpenTelemetry на сервере с `2.0.0` (метрики, трейсы, логи в Grafana/Tempo/Loki).
- Feature flags (Unleash или собственное минимальное решение) с `2.0.0` — для постепенного раскатывания рисковых фич.

### 10.4. Качество кода

- Roslyn analyzers с уровня warning-as-error.
- Code coverage минимум 60% для Application/Domain слоёв, 40% для Infrastructure.
- Architecture tests (NetArchTest или ArchUnitNET) — проверяют, что Presentation не зависит от Infrastructure напрямую.

### 10.5. Документация

- `docs/` синхронизируется с релизами: каждый минор обновляет [architecture.md](architecture.md) и добавляет changelog-запись.
- Публичная документация на `docs.musicbakh.app` начинается с `2.0.0`.
- Каждый минор имеет дизайн-документ в `docs/superpowers/specs/YYYY-MM-DD-N.N.0-<тема>-design.md`.

### 10.6. Локализация

- i18n-инфраструктура с `1.4.0` (resx + сатисфакторно или Polyglot).
- Запас под добавление языков без правки кода. Языки на старте: RU, EN. Заготовки под UK, KZ, DE, ES.
- Community-translation через Crowdin/Weblate — в касте «Возможно потом».

---

## 11. Сводная карта (на одном экране)

```
1.0.0  ─── Release (current)

—— ФАЗА A: local-first Windows player ──────────────────────────────────────
1.1.0  Library 2.0           SQLite + чистые слои, FTS5, рейтинги, теги, плейлисты, watched
1.2.0  Audio Engine 2.0      FLAC/OGG/Opus/AAC, gapless, crossfade, ReplayGain, EQ
1.3.0  Metadata & Lyrics     tag editor, AcoustID, LRC, duplicates  (через provider-интерфейсы)
1.4.0  UX polish             темы, mini-player, OSD, tray, hotkeys, i18n
1.5.0  Stats + Scrobble      дашборд, Year-in-Music, Last.fm, Discord RP
1.6.0  Podcasts + Radio      RSS, radio-browser, личное радио
1.7.0  Internal providers    формализованные внутренние модули (НЕ plugin loading)
1.8.0  Installer + updater   Velopack, Authenticode, crash logs
1.9.0  Architecture hard.    DI, MVVM toolkit, Serilog, CI, FlaUI

—— ФАЗА B: backend + companions ────────────────────────────────────────────
2.0.0  Backend MVP           ASP.NET + Postgres + JWT, self-host, REST/OpenAPI
2.1.0  Sync v1               revision-based, last-write-wins, soft delete, manual conflict UI
2.2.0  Self-host cloud       S3, HLS, NAS-источники (БЕЗ hosted storage)
2.3.0  Web PWA companion     library UI + HTML5 player + share-links (metadata-only)
2.4.0  Subsonic API          вся чужая мобильная экосистема бесплатно
2.5.0  Mobile companion      login, browse, remote, базовый стрим с self-host
2.6.0  Reco v1               content-based + Last.fm similar + Daily Mix + Personal Radio
2.7.0  Scrobble + соц-lite   публичные профили, follow, privacy, импорт Last.fm
2.8.0  Connect-transfer      передача воспроизведения между своими устройствами

—— ФАЗА C: social-lite + AI + reco v2 ──────────────────────────────────────
3.0.0  Social-lite           лента, collab playlists (CRDT точечно), музыкальная совместимость
3.1.0  Listening Rooms v1    host-очередь + приблизительный sync + чат
3.2.0  AI Assistant          промпт-плейлисты, объясни-трек, mood-теги, голос
3.3.0  Reco v2               embeddings + pgvector + similar + объяснимость
3.4.0  Public API            OAuth scopes, webhooks, HA, Telegram/Discord
3.5.0  Creative-lite         lyrics-карточки, recap, Heardle, AutoEQ, CD-rip

—— ЗА ГОРИЗОНТОМ ───────────────────────────────────────────────────────────
Возможно потом: plugin marketplace, Avalonia, E2E private mode, multi-room,
                two-tower reco, ActivityPub, ASIO/DSD, watches, stems
Не соло:        соцсеть, hosted storage, marketplace, artist platform,
                federation, Sonos-уровень sync, Spotify ML, full mobile parity
Никогда:        NFT, voice cloning, MQA, VR-клуб, watermarking, blockchain
```

---

## 12. Условия пересмотра

Этот документ — стратегический, но не священный. Поводы для пересмотра:

- **Появление контрибуторов / команды** → можно поднимать вверх касту «Возможно потом».
- **Резкое изменение технологического ландшафта** (например, выпуск зрелого open-source two-tower фреймворка, который снимает риск).
- **Юридические сдвиги** (например, изменение DMCA-аналогов или появление законных схем для hosted-стриминга в РФ).
- **Реальный пользовательский спрос** на фичу из касты «Возможно потом» — критерий: значимое количество запросов в issue tracker.
- **Через каждые 5 миноров** — короткий ретроспектив-пересмотр: что реально получилось, какие оценки сбылись, что переоценено.

Любой пересмотр оформляется через diff к этому файлу с пояснением в commit message, а не «правкой по ходу».
